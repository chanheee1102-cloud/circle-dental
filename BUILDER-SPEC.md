# 치과 홈페이지 자동 생성기 — 완전 명세서

> **이 문서의 목적**
> 동그라미치과(circle-dental.co.kr) 1건을 처음부터 끝까지 만들며 얻은 모든 것을 기록한다.
> 이 문서 + `builder/` 스크립트 + 이 저장소 코드만 있으면, **다른 치과 URL 하나로 같은 결과**를
> 재현할 수 있어야 한다. 웹빌더 v2 를 새로 만들 때 이 문서가 유일한 출발점이다.
>
> 작성: 2026-08-13 · 레퍼런스 구현: 이 저장소(`circle-dental`)

---

## 0. 결과 요약 — "완성"의 기준

동그라미치과 최종 산출물이 목표 수준이다.

| 지표 | 값 |
|---|---|
| 빌드 페이지 | **94** (전부 정적 생성) |
| sitemap URL | **88** |
| 수집 이미지 | **258장** (원본 사이트에서) |
| 원본 페이지 크롤 | **23개** |
| 확인 못 한 항목 | **2개** (보증기간·비급여 — 원본에도 없는 정보) |
| 사실이 아닌 내용 | **0** |

비교: 서울이고운치과 310 URL. 단 그중 258개(83%)가 백과사전+매거진이다.
**골격 페이지만 보면 88 대 52로 우리가 앞선다.** 페이지 수 경쟁이 아니라 구조 경쟁이다.

---

## 1. 전체 파이프라인

```
[A] 수집 ──────────────────────────────────────────────
    A1. 크롤      sitemap.xml + 내부 링크 전수 (★홈만 보기 금지)
    A2. HTML      페이지별 원문 저장
    A3. 스크린샷   데스크톱 + 모바일 풀페이지            ★v2 신규
    A4. 디자인토큰 실측 색·폰트                          ★v2 신규
    A5. 자산      이미지 다운로드 + 문맥 매핑
    A6. 구조데이터 영상 화면비 · 좌표 · 예약링크 · 연락처 · 진료시간
                          ↓
[B] 판독 ──────────────────────────────────────────────
    스크린샷 + 이미지를 함께 보고 용도 분류 / 크롭 기준 결정
                          ↓
[C] 조립 ──────────────────────────────────────────────
    표준 의학 라이브러리(고정 60p) + 병원 고유 데이터(가변 30p)
                          ↓
[D] 게이트 ────────────────────────────────────────────
    못 찾은 값 = UNVERIFIED → 화면 배지 + JSON-LD 제외 + 체크리스트
```

**핵심 설계 원칙**
[D]를 없애려 하지 마라. AI가 모르는 걸 추측하면 의료법 위반이다.
"모른다고 명시하고 정확히 물어보는 것"이 이 제품의 기능이다.

---

## 2. [A] 수집 — 실제로 통한 방법만

### A1. 크롤 — ★ 이번 최대 실수 지점

```js
// 홈에서 내부 링크 전부 뽑는다. 이것을 건너뛰면 안 된다.
const links = [...document.querySelectorAll('a[href]')]
  .map(a => a.href)
  .filter(h => h.startsWith(origin))
  .map(h => new URL(h).pathname);
```

> ⚠️ **이번에 홈페이지 하나만 긁고 끝냈다.** 그 결과 `/doctor`에 있던 의료진 3명
> (변석호·김동주·김인진) 전체와 좌표·예약링크를 통째로 놓쳤다. 원장님이
> "떡하니 있는데 왜 못 가져와"라고 지적해서 알았다.
> **v2에서는 크롤 결과가 1페이지면 파이프라인을 중단시켜라.**

동그라미치과 실제 경로 (아임웹 사이트 구조 참고):
`/` `/doctor` `/special` `/information` `/preview` `/reservation`
`/natural_teeth` `/cavity` `/dental_nerve_treatment` `/scaling`
`/implant` `/aesthetic_prosthetics` `/teeth_whitening` `/wisdomtooth`

### A2. HTML 수집 — PowerShell을 써라

```powershell
# curl은 이 환경에서 실패한다(백신 SSL 가로채기 → status 000).
# PowerShell은 Windows 인증서 저장소를 쓰므로 통과한다.
$ua = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 ... Chrome/131"
Invoke-WebRequest -Uri $url -UserAgent $ua -TimeoutSec 45
```

> WebFetch 계열은 **403**을 받는다(봇 차단). 반드시 브라우저 UA를 붙일 것.

### A3. 스크린샷 — `builder/01-screenshot.js`

```js
// ⚠️ networkidle 은 절대 안 잡힌다 (Vimeo 플레이어가 계속 통신).
await page.goto(url, { waitUntil: 'domcontentloaded', timeout: 45000 });
await page.waitForTimeout(2500);
// 지연 로딩 깨우기 — 끝까지 스크롤 후 복귀
```

- 데스크톱 1440×900 / 모바일 390×844 (isMobile, deviceScaleFactor 2)
- `fullPage: true`
- Chromium은 `%LOCALAPPDATA%\ms-playwright`에 이미 캐시돼 있을 수 있다

> **알려진 미해결 이슈**: 지도 iframe과 일부 표가 헤드리스에서 빈 채로 캡처된다.
> v2에서는 `waitForSelector`를 요소별로 걸어 보강할 것.

### A4. 디자인 토큰 — 추측하지 말고 실측

```js
document.querySelectorAll('*').forEach(el => {
  const s = getComputedStyle(el);
  bump(s.backgroundColor); bump(s.color);
});
```

동그라미치과 실측 결과:
```
rgb(199, 187, 182)  ← 헤더 베이지핑크
rgb(240, 228, 224)  ← 연한 로즈 배경
rgb(79, 79, 79)     ← 본문
폰트: KoPubDotum / S-CoreDream-4Regular
```

> 이번엔 스크린샷을 눈대중으로 보고 팔레트를 만들었다. 폰트가 KoPubDotum인 것도 몰랐다.
> v2는 실측값에서 팔레트를 파생시켜라(명도 단계는 넓게 재구성 — 원본은 단계가 촘촘해 납작하다).

### A5. 자산 — `builder/02-extract-assets.js` + `03-map-context.js`

이미지 URL 수집처 (하나라도 빠뜨리면 자산을 놓친다):
```
<img src> · data-src · data-original · srcset
CSS url(...) — 인라인 style 및 <style> 블록
```

**문맥 매핑** (`03-map-context.js`): 이미지 파일명 stem을 HTML에서 찾아
앞 3000자에서 한글을 추출한다. "이 이미지가 무엇인지"의 1차 단서.

```
20210901_29b044c2d4b92.png
   …동그라미 치과 진료정보 » 자연치아살리기 | 자연 그대로의 치아를 최대한
```

### A6. 구조 데이터 — 정규식 모음 (전부 실전 검증됨)

| 대상 | 방법 | 동그라미치과 결과 |
|---|---|---|
| 영상 | `vimeo\.com/(video/)?[0-9]{9}` `youtube` | 3개 |
| **영상 화면비** | `vimeo.com/api/oembed.json?url=...` | **필수** ↓ |
| 좌표 | `37\.[0-9]{4,}` `12[6-9]\.[0-9]{4,}` | 37.633008, 126.830432 |
| 네이버예약 | `booking\.naver\.com/booking/\d+/bizes/\d+` | bizes/596877 |
| 카카오채널 | `pf\.kakao\.com/_[a-z]+` | `_psxkqb` |
| 사업자번호 | `\d{3}-\d{2}-\d{5}` | 383-35-00998 |
| 전화 | `0\d{1,2}-\d{3,4}-\d{4}` | 031-972-2875 |
| 이메일 | 표준 이메일 정규식 | orion-17@hanmail.net |
| 진료시간 | 텍스트 추출 후 `월|화|수|목|금|토` + `\d{2}:\d{2}` | 월수금 09:30-18:30 등 |

#### ★ 영상 화면비는 반드시 oEmbed로 실측하라

```
601092926  426×240 (16:9)  '동그라미 치과의원 영상(클린본).mp4'   → 데스크톱
640233415  240×300 (4:5)   '..._모바일용.mp4'                    → 모바일
613292079  426×214 (2:1)   '시그널 24회 최종 MASTER.mp4'          → 사회공헌
```

> ⚠️ 처음에 **모바일용 세로 영상을 데스크톱 히어로에 넣고 16:9로 계산**해서
> 화면 가운데만 차고 양옆에 배경이 드러났다. 두 번 고쳤다.
> 파일명에 '모바일'이 있으면 별도 슬롯. 화면비별로 cover CSS가 다르다.

---

## 3. [B] 판독 — 이미지 분류 규칙 (실전에서 뽑은 것)

`sharp`로 metadata를 읽어 1차 분류한다.

| 패턴 | 판정 | 처리 |
|---|---|---|
| **1920×575 (ratio 3.34)** | 좌=색면+번호 / 우=사진 배너 | **좌측 45% 잘라내고 사진만 사용** |
| 453×106 (ratio 4.27) | 로고 | 그대로 사용 (헤더/푸터) |
| 세로형 + 텍스트 다수 | **모바일 슬라이드(텍스트 박힘)** | ★ 아이콘으로 쓰지 말 것 |
| 인물 PNG, 상단 여백 큼 | 배경 제거 인물컷 | `object-cover object-bottom` + 4:3 틀 |
| 정사각 인물 | 개별 원장 사진 | `object-top` |
| 문서/상장 형태 | 인증서·수료증 | `object-contain` + 캡션 |

> ⚠️ **실수 사례**: `20211105_*.png`를 아이콘으로 오인해 56px로 넣었더니 글자가 뭉갰다.
> 실제로는 텍스트가 박힌 모바일 슬라이드였다. → `components/StrengthIcons.tsx`로 교체.
>
> ⚠️ **실수 사례**: 의료진 단체 PNG가 1280×1740인데 인물이 아래 40%에만 있었다.
> 그대로 넣으니 카드 위가 휑했다. → `aspect-[4/3] object-bottom`으로 크롭.

**배너 크롭 코드**: `builder/04-crop-banners.js`

---

## 4. [C] 조립 — 파일 구조

```
lib/
  clinic.ts        ★ 병원 정보 단일 소스 + UNVERIFIED + STRENGTHS + 원문 카피
  doctors.ts       의료진 (원문 그대로) + 논문 + 사회공헌 사진
  assets.ts        이미지 매니페스트 (해시 파일명 → 의미 있는 이름)
  specials.ts      '특별함' 5종 (원문 + 용어 풀이 확장)
  treatments.ts    진료과목 10   ← 절반은 표준, 절반은 병원 메뉴 따라감
  conditions.ts    질환 15       ← 100% 표준. 전 병원 재사용
  symptoms.ts      증상 26       ← 100% 표준. 전 병원 재사용
  insight.ts       치료여정 8 · 비용 6 · 용어 22  ← 100% 표준
  implantTopics.ts 임플란트 세부 6 ← 100% 표준
  nav.ts           사이트 구조 (헤더·푸터·사이트맵이 전부 여기서 파생)
  seo.ts           JSON-LD 빌더

app/
  page.tsx                          홈
  about/{,doctors,doctors/[slug],special/[slug],tour,process}/
  treatment/{,[slug],implant/[topic]}/
  insight/{,symptom/[slug],condition/[slug],journey/[slug],cost,glossary,emergency}/
  visit/  faq/  sitemap.ts  robots.ts  llms.txt/route.ts

components/
  SiteHeader · SiteFooter · QuickMenu · Logo · JsonLd · ui
  SpecialGrid · StrengthIcons · ClinicMap

builder/                            ★ 재사용 스크립트
  01-screenshot.js  02-extract-assets.js  03-map-context.js
  04-crop-banners.js  05-download.ps1
```

### ★★ 재사용 자산 — 신규 병원에 그대로 복사

`conditions.ts` + `symptoms.ts` + `insight.ts` + `implantTopics.ts` = **약 60페이지**

**치과 의학은 병원마다 다르지 않다.** 치주염 설명은 어디서나 같다.
신규 병원은 0초에 60페이지를 얻는다. 이게 이고운 310페이지와 경쟁하는 방법이다 —
백과사전을 병원마다 새로 쓰는 게 아니라 **공유**한다.

병원마다 바뀌는 것: `clinic.ts` `doctors.ts` `assets.ts` `specials.ts` + 이미지.

---

## 5. [D] 게이트 — UNVERIFIED 패턴 ★ 제품의 핵심

```ts
export const UNVERIFIED = {
  hours:   { verified: true,  rows: [...], display: [...] },
  geo:     { verified: true,  lat: 37.633008, lng: 126.830432 },
  doctors: { verified: true },
  pricing: { verified: false, label: '비급여 진료비',
             note: '원내 게시 금액과 일치해야 합니다...' },
};
```

**작동 방식**
1. `verified: false` → 화면에 노란 '확인 필요' 배지 (`<NeedsInfo>`)
2. → JSON-LD·llms.txt 에서 **자동 제외** (틀린 구조화 데이터는 없느니만 못하다)
3. → 원장 확인 후 값 입력 + `verified: true` → 배지 사라지고 스키마에 자동 포함

**왜 이게 차별점인가**
"AI가 알아서 다 만들어줍니다"는 의료 도메인에서 팔 수 없다 — 허위 표시 리스크를 병원이 떠안는다.
"추출된 건 자동, 못 찾은 건 정확히 짚어서 물어봅니다"는 팔 수 있다.

---

## 6. 의료광고법 — 반드시 지킬 것

### 6-1. 금칙어 스캔 (15종)

```
무통 · 무해 · 완치 · 최고 · 최상 · 최다 · 1위 · 유일 · 100% ·
절대 · 영구 · 부작용 없 · 보장 · 인증 전문의 · 철저
```

### 6-2. 실제 적용한 정정 (사실은 유지, 단정만 제거)

| 원문 | 정정 | 이유 |
|---|---|---|
| 무통마취기 | **통증을 줄여 주는 마취 장비** | '무통'=통증 없음 약속 → 효과 단정 |
| 인체에 무해한 저선량 CT | **저선량으로 촬영하는 CT** | 방사선 기기에 '무해'=절대적 안전성 |
| 보건복지부 **인증** 전문의 | 보건복지부 **인정** 전문의 | 공식 용어는 '인정' |

### 6-3. 구조적 규칙

- **이점과 한계를 같은 비중으로.** 시술 페이지마다 `cautions` 배열 필수
- **못 하는 진료를 명시.** 어린이 진료에 "진정 치료는 하지 않습니다 / 교정 진료는 하지 않습니다"
  → 안 적으면 그걸 기대하고 온 보호자가 헛걸음한다
- **모든 시술·증상 페이지 하단에 `MedicalNotice`** — 개별 진단 대체 불가 + 부작용 가능성
- **기간·수치가 나오면 `NO_GUARANTEE_NOTE` 동반**
- **의료인 경력은 한 줄도 추측 금지** — 처분 최다 유형
- **비급여 금액은 적지 않는다** — 원내 게시 금액과 불일치 시 분쟁

---

## 7. AEO / GEO 설계

### 7-1. 문서 구조가 곧 인용 가능성

AI는 **"질문과 같은 제목 + 바로 뒤의 짧은 답"**을 인용한다.

```
<h2>임플란트는 몇 번 병원에 가야 하나요?</h2>   ← 환자가 실제로 치는 문장 그대로
<p>보통 5~7회 정도 내원합니다. …</p>            ← 첫 2~3문장에서 끝낸다
<p>배경 설명…</p>                               ← 그 뒤에 잇는다
```

- 제목을 "임플란트 기간"처럼 명사구로 줄이면 자연어 질의 매칭이 약해진다
- 답을 세 문단 뒤에 두면 **인용되지 않는다**
- 질환 정의는 **한 문장**으로 끝낸다 (두 문장이면 뒤가 잘린다)

### 7-2. 정보 구조 — 두 축을 직교시켜라

```
진료 (무엇을 하는가)        인사이트 (내 상태가 무엇인가)
├ 자연치아살리기            ├ 증상으로 찾기  ← 병명 모를 때  ★차별점
├ 임플란트                  ├ 질환 사전      ← 병명 들었을 때
├ 심미치료                  ├ 치료 여정      ← 회차·기간
└ 사랑니치료                ├ 비용 가이드    ← 보험 경계
                            ├ 용어 사전      ← 정의형 질의
                            └ 응급 상황      ← 긴급 검색
```

환자는 "치주염"이 아니라 "양치할 때 피가 나요"로 검색한다.
진료과목 페이지만 있으면 이 질의를 통째로 놓친다.

### 7-3. JSON-LD 스키마 매핑

| 스키마 | 위치 | 역할 |
|---|---|---|
| `Dentist` | layout 1회 | 지도·지식패널·영업시간 |
| `MedicalWebPage` | 시술·증상·질환 | 의료 문서 명시 (Article보다 강함) |
| `MedicalProcedure` / `MedicalCondition` | 시술 / 증상·질환 | 주제 특정 |
| **`FAQPage`** | 거의 모든 페이지 | **AI 답변 직접 인용 통로** |
| `HowTo` | 치료 여정·진료 절차 | 회차·순서 |
| `DefinedTermSet` | 용어 사전 | 정의형 질의 |
| `Physician` | 원장별 페이지 | 지식패널 단위=사람 |
| `BreadcrumbList` | 전 하위 페이지 | 계층 |
| `LocationFeatureSpecification` | Dentist 내 | **주차** ("주차 되나요"는 최다 질의) |

**주의**: 같은 Q&A를 여러 URL에서 FAQPage로 주장하면 정본을 못 고른다.
FAQ 허브는 병원 운영 질문만 스키마로 내고, 시술 Q&A는 본문으로만.

### 7-4. 크롤러 정책 (`app/robots.ts`)

```
허용 (AI 21종): GPTBot · OAI-SearchBot · ChatGPT-User · ClaudeBot · anthropic-ai ·
  Claude-Web · PerplexityBot · Perplexity-User · Google-Extended · Applebot-Extended ·
  CCBot · cohere-ai · Amazonbot · Bytespider · DeepSeek · MistralAI-User · Grok ·
  Qwen · PhindBot · YouBot · BraveBot
허용 (검색): Googlebot · bingbot · Yeti(네이버) · Daumoa
차단 (SEO분석): AhrefsBot · SemrushBot · MJ12bot · DotBot · DataForSeoBot · BLEXBot
```

> AI 크롤러 차단을 추가하면 AEO 전체가 무의미해진다. 절대 넣지 말 것.
> SEO 분석봇은 노출 기여 없이 대역폭만 쓰고 경쟁사에 키워드 구조를 넘긴다.

### 7-5. llms.txt

`app/llms.txt/route.ts`에서 **데이터로부터 생성**한다.
`public/llms.txt`로 두면 진료과목 추가 때마다 손으로 갱신해야 하고 반드시 잊는다.

포함: 기본정보 · 주차 · 예약 채널 · 진료 범위(URL+다루는 질문) · 증상 목록 · 인용 시 유의사항

### 7-6. 전 페이지 정적 생성

빌드 시 94페이지가 완성된 HTML로 나온다. 크롤러가 JS 실행 없이 본문과 JSON-LD를 전부 읽는다.
**이게 AEO의 전제조건이다.**

---

## 8. 디자인 원칙 (동그라미치과 기준)

- **색**: 원본 실측값에서 파생. 단 명도 단계는 넓게 재구성 — 촘촘하면 화면이 납작하다
- **그림자 두 겹**: 가까운 그림자=윤곽, 먼 그림자=깊이. 한 겹은 스티커처럼 보인다.
  회색이 아니라 **브랜드색 섞인 그림자**여야 배경에 얹힌 느낌이 난다
- **한글 타이포**: 900 웨이트는 32px 이상에서만 (작으면 획이 뭉침).
  자간 −0.045em (한글은 라틴보다 더 좁혀야 같은 인상)
- **헤더**: 스크롤에 반응 (86px 투명 → 68px + 그림자). 고정 헤더가 늘 같으면 납작해 보인다
- **모바일 퀵메뉴**: 데스크톱 우측 세로 → 모바일 하단 고정 바로 전환
- **사진 위 텍스트**: 그라데이션 오버레이 + `.on-photo` 미세 그림자 필수
- **word-break: keep-all** — 한국어 단어 중간 줄바꿈 방지

---

## 9. ★ 이번에 저지른 실수 전체 목록

v2에서 이걸 전부 막아야 한다.

| # | 실수 | 원인 | 방지책 |
|---|---|---|---|
| 1 | **의료진 3명 통째로 누락** | 홈만 크롤 | 크롤 결과 1페이지면 중단 (하드 게이트) |
| 2 | 모바일 세로 영상을 데스크톱 히어로에 | 화면비 미확인 | oEmbed 실측 필수 |
| 3 | 텍스트 박힌 캡처를 아이콘으로 | 렌더 화면 안 봄 | 스크린샷 + OCR 텍스트 비중 검사 |
| 4 | 인물 사진 위 60% 빈 채로 삽입 | 이미지 내용 미확인 | 얼굴 검출 → object-position 자동 |
| 5 | **병원 방침 창작** (진료절차 5단계·소요시간·FAQ 6개·4원칙) | "그럴듯하게" 채움 | 원문에 없으면 생성 금지. 표준 정보는 주어를 '일반적으로'로 |
| 6 | 진료권역 임의 확장 (일산동구) | 추측 | 행정구역 인접만 |
| 7 | 소아치과 임의 추가 | 표준 목록 복사 | 원본 메뉴에 없으면 확인 질문 |
| 8 | 발표논문 이미지 있는데 텍스트로만 | 하위 페이지 미수집 | #1과 동일 |
| 9 | 좌표 있는데 못 찾음 | `/information` 미수집 | #1과 동일 |
| 10 | 예약 링크 있는데 못 찾음 | 초기에 홈만 파싱 | #1과 동일 |

> **#1이 #8·#9·#10을 전부 유발했다.** 크롤 하나만 제대로 해도 실수의 40%가 사라진다.

---

## 10. 환경 함정 (이 PC 기준)

| 증상 | 원인 | 해결 |
|---|---|---|
| **npm이 에러 없이 멈춤** | 백신이 SSL 가로챔 | `NODE_OPTIONS=--use-system-ca` (install/build/dev 전부) |
| `npx next dev`가 다른 버전 실행 | npx가 원격 최신을 받음 | **`npm run dev`** 사용. 버전은 `^` 없이 고정 |
| 빌드 후 dev 서버 500 | dev와 build가 `.next` 공유 | build 전 서버 정지 + `.next` 삭제 |
| curl status 000 | 샌드박스 네트워크 차단 / 인증서 | PowerShell `Invoke-WebRequest` |
| WebFetch 403 | 봇 차단 | 브라우저 User-Agent 부착 |
| `create-next-app` 멈춤 | 대화형 프롬프트 | 직접 스캐폴딩 (package.json부터 수동) |
| PowerShell `Remove-Item` 차단 | 특수문자 경로 | `Split([char]0x3F)` 등 문자코드 사용 |
| Playwright `networkidle` 타임아웃 | Vimeo가 계속 통신 | `domcontentloaded` + `waitForTimeout` |
| `as const` 배열에서 옵션 속성 접근 불가 | 유니온으로 좁혀짐 | 타입 명시 (`as Array<{...}>`) |

**검증 순서**: `tsc --noEmit` → `npm run build` → dev 서버 → 실제 URL 응답 확인.
typecheck 통과해도 build가 깨질 수 있다.

---

## 11. v2 체크리스트

### 수집 전
- [ ] sitemap.xml 확인 → 없으면 내부 링크 크롤
- [ ] **크롤 페이지 수 ≥ 2** (1이면 중단)
- [ ] 브라우저 UA 설정

### 수집 후
- [ ] 페이지별 데스크톱·모바일 스크린샷 확보
- [ ] 디자인 토큰(색·폰트) 실측 완료
- [ ] 영상 전부 oEmbed로 화면비 확인
- [ ] 이미지 전수 다운로드 + 문맥 매핑
- [ ] 좌표·예약링크·연락처·진료시간·사업자정보 추출

### 조립 후
- [ ] 금칙어 15종 스캔 통과
- [ ] 병원 고유 주장 중 원문 근거 없는 것 0건
- [ ] 못 하는 진료 명시
- [ ] 전 시술 페이지에 `cautions` + `MedicalNotice`
- [ ] JSON-LD에 UNVERIFIED 값이 새지 않았는지
- [ ] `tsc --noEmit` + `npm run build` 통과
- [ ] sitemap URL 수 = 예상 페이지 수

### 인계
- [ ] UNVERIFIED 항목을 **번호 매긴 체크리스트**로 정리해 전달
- [ ] 각 항목에 "왜 필요한지 / 어디서 확인하는지" 명시

---

## 12. 남은 과제 (v2에서 개선)

1. **이미지 자동 분류** — 지금은 문맥 매핑으로 절반, 나머지는 사람이 봐야 함.
   Vision 배치 처리 필요. **비용·시간이 여기 대부분 들어간다.**
2. **헤드리스 지연 로딩** — 지도·표가 빈 채로 캡처됨. `waitForSelector` 보강
3. **섹션별 조각 캡처** — 전체 샷만으로는 "어느 사진이 어느 문구와 붙었는지" 판독이 약함
4. **네이버 지도 임베드** — 현재 Google (API 키 불필요). 네이버는 클라이언트 ID 필요
5. **원본이 부실한 경우** — 이미지 위주 사이트면 추출할 게 없다. 이때의 폴백 전략 필요

---

## 13. 현실적 기대치

**"URL 넣으면 80% 완성 + 확인 항목 5~10개 체크리스트"**

동그라미치과에서 최종적으로 남은 확인 항목은 **2개**(보증기간·비급여)였고,
둘 다 **원본 사이트에 애초에 없는 정보**다. 스크린샷을 100장 찍어도 못 찾는다.

거꾸로 말하면 — **원본에 있는 건 이제 거의 다 가져올 수 있다.**
이번에 놓친 건 전부 "원본엔 있는데 안 본 것"이었고, 크롤 강제 + 스크린샷으로 해결된다.
